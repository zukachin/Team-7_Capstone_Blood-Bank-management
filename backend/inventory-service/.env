PORT=4001

# -----------------------------
# Database Config (Postgres)
# -----------------------------
# Format: postgres://<username>:<password>@<host>:<port>/<dbname>
PG_HOST=localhost
PG_USER=postgres
PG_PASSWORD=
PG_DATABASE=
PG_PORT=5432
DONOR_BASE = 'http://localhost:4000'

JWT_SECRET=
MASTER_KEY=

OTP_TTL_MINUTES=10
BCRYPT_SALT_ROUNDS=10
JWT_EXPIRES_IN=7d

# SMTP settings (production)
 SMTP - if empty and NODE_ENV != 'production' will use Ethereal
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false        # true for port 465, false for 587 (STARTTLS)
SMTP_USER=
SMTP_PASS=
EMAIL_FROM=

# or for dev/test using Ethereal you don't set SMTP_* and will use createTestAccount (optional)


NODE_ENV=development
OTP_TTL_MINUTES=10
